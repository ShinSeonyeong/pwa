// push, pop
const arr = [10, 20, 30];

const pushValue = arr.push(40);
console.log(pushValue); // 4 (리턴되는 값은 길이가 나옴)
console.log(arr);

const popValue = arr.pop();
console.log(popValue);
console.log(arr);